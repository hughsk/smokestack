{
  "name": "smokestack-tape",
  "private": true,
  "scripts": {
    "test": "browserify test.js | smokestack --tap | tap-spec"
  },
  "devDependencies": {
    "browserify": "^6.3.2",
    "tap-spec": "^1.0.1"
  }
}
